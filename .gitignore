# ── Credentials — NE JAMAIS PUSHER ──
*.json
freeride-automation-*.json

# ── Config locale (IDs / URLs privées) ──
config.py

# ── Photos riders (données personnelles) ──
PPRIDERS/
PPRiders/
PictureRiders/

# ── Fichiers de test temporaires ──
test_*.png
eq_card_preview.*

# ── Données / fichiers générés ──
output/
flags/
*.xlsx
*.xls

# ── Cache Python ──
__pycache__/
*.pyc
*.pyo

# ── macOS ──
.DS_Store
.AppleDouble
.LSOverride

# ── Logs / divers ──
*.log
venv/
.env
env/

# ── Scripts helper temporaires ──
*.command
rebuild_riders.py

# ── Photos produits locales (binaires, trop lourdes pour git) ──
Equipment/
