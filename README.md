# FreerideFanatics — UCI MTB World Cup DH kit tracker

Static site: one directory page + one detail page per rider, listing their
full race setup with "Shop" buttons for affiliate links. Design language is
adapted from hotcut.xyz (warm light background, wavy line-art decoration,
condensed headlines, mono labels, burnt-orange accent).

## Structure

```
sync.py                   — downloads the Google Sheet, regenerates data/riders.json, rebuilds
import_images.py          — imports + resizes photos from ~/Desktop/freeride, rebuilds
build.py                  — generator: reads data/riders.json, writes index.html + riders/*.html
data/riders.json          — generated by sync.py (do not hand-edit if you sync)
assets/css/style.css      — shared stylesheet
assets/js/site.js         — header, mobile nav, scroll reveal, FAQ, grid filters, equipment marquee
assets/img/riders/        — square avatars, generated by import_images.py
assets/img/riders-action/ — portrait action shots, generated by import_images.py
assets/img/equipment/     — product photos, generated by import_images.py
index.html                — generated, do not hand-edit
riders.html               — generated, do not hand-edit
riders/{slug}.html        — generated, do not hand-edit
```

Two independent sources feed the site, each with its own command: the Google
Sheet for data (`sync.py`) and the Desktop photo library for images
(`import_images.py`). Both end by rebuilding the HTML.

## Updating the site

**The Google Sheet is the source of truth.** One command pulls it and
rebuilds everything:

```bash
python3 sync.py
```

It reads `UCI_DH_2026_Tracker_v3` (sheet id in `sync.py`, exported via the
public link-share URL — no credentials needed) and maps:

| Sheet tab | Feeds |
| --- | --- |
| `🔧 Equipment Women` / `🔧 Equipment Men` | each rider's parts, keyed by Instagram handle |
| `👤 Profils` | name, country, hometown, DOB, team, sponsors, palmarès |
| `📊 Résultats 2026` | country code + UCI points per round (drives all rankings) |
| `equipment link` | product URL per part → the "Shop" buttons; optional `Photo` column |

Options:

```bash
python3 sync.py --no-build          # refresh data/riders.json only
python3 sync.py --offline file.xlsx # parse a local export instead of downloading
python3 build.py                    # rebuild from existing riders.json (no network)
```

Never hand-edit `index.html`, `riders.html` or files in `riders/` — they are
overwritten on every build.

## Photos

The photo library lives outside the project, at `~/Desktop/freeride/`, and
keeps its own naming (files named by Instagram handle, equipment sorted into
category folders). **Don't reorganise it** — one command imports, resizes and
renames everything into the site:

```bash
python3 import_images.py
```

| Library folder | Becomes | Used for |
| --- | --- | --- |
| `PPRiders/@handle.jpg` | `assets/img/riders/{slug}.jpg` (400×400) | avatars on the rider grid |
| `PictureRiders/@handle.jpg` | `assets/img/riders-action/{slug}.jpg` (max 900px) | hero on the rider detail page |
| `Equipment/{Category}/Brand;Model.webp` | `assets/img/equipment/{cat}-{brand}-{model}.jpg` (max 600px) | thumbnails on rider pages + Best Equipment carousel |

Riders are matched on the Instagram handle already in `riders.json`, so messy
filenames (trailing space, capitals, missing `@`) still resolve. Equipment is
matched per category against the products actually used, and the imported
filename is derived from the *product*, so the site's lookup stays exact.

Options:

```bash
python3 import_images.py --report     # what would match, writes nothing
python3 import_images.py --force      # re-process files already imported
python3 import_images.py --no-build
```

The run ends with the list of products still missing a photo, giving the
filename to add and the category folder to drop it in.

### Frame reveal
On a rider page the frame is the centrepiece of the chassis composition and
gets a three-stage reveal when scrolled into view: a line drawing traces
itself, becomes a pencil sketch, then resolves into the photo. A replay
button appears in the panel corner afterwards.

Both extra layers are **derived from the frame photo** by `import_images.py`
(needs `opencv-python`; the step is skipped with a notice if it's missing):

```
assets/img/equipment/reveal/{frame-slug}-sketch.png   dodge-blend pencil render
assets/img/equipment/reveal/{frame-slug}-draw.svg     Canny contours as self-drawing paths
```

Add a new frame photo and rerun `python3 import_images.py` — both layers are
generated automatically, no hand-authored assets. Use `--force` to regenerate
after tuning. Frames without these files simply show the plain photo, and the
whole sequence is disabled under `prefers-reduced-motion`.

### Why the category is in the equipment filename
28 products share a brand and model across different categories — "Shimano
Saint" is a brake lever, a crankset, a derailleur *and* a pedal. Without the
category prefix they would all collide onto a single photo, so the site would
show a pedal where a derailleur belongs. This is also why the library's
category folders must stay: flattening them into one directory would throw
away the information that keeps those products apart.

### Adding photos by hand
Dropping a correctly-named file straight into `assets/img/...` works too —
the build picks it up with no code or JSON change, and falls back gracefully
when a file is absent (initials avatar for riders, no thumbnail for
equipment). To see every filename the build is looking for:

```bash
python3 build.py --missing-images
```

You can also fill the `Photo` column in the sheet's `equipment link` tab with
an image URL — `sync.py` downloads it under the right name on the next run.

## Known gaps

- 4 riders have no photo in the library (Danny Hart, Kate Hastings, Fernando
  Juan Muñoz, Oisin O'Callaghan) and fall back to the initials avatar.
- 60 of 263 equipment products have a photo. Of the 203 missing, ~74 are in
  categories whose library folder is still empty (Tires, Saddle, Shoes,
  Helmet, Protection, Goggles) and ~92 have no file for that brand. Run
  `python3 import_images.py --report` for the exact list.
- Some library filenames don't follow the `Brand;Model` convention (raw
  camera/CDN names like `2701657.png`); matching is deliberately strict, so
  those stay unmatched rather than risk showing the wrong product.
- 285 of 642 equipment items have a shop link; the rest have no matching row
  in the `equipment link` tab and fall back to `#`.
- 3 riders (Danny Hart, Fernando Juan Muñoz, Oisin O'Callaghan) are absent
  from the `📊 Résultats 2026` tab, so they have no points and no country code.
- 2 riders (Stefano Introzzi, Antoine Rogge) have a profile but no equipment
  spec in the sheet.
- No logo/brand assets from the Figma file yet — header uses a text wordmark.
